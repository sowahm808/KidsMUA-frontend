# Contributing
Use conventional commits (`feat:`, `fix:`, `docs:`). Run `npm run format`, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build:production` before opening a pull request.
