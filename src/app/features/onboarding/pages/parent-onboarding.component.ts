import { CommonModule } from '@angular/common';import { Component, signal } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<form class="card" [formGroup]="form" novalidate><h1 class="text-3xl font-bold">Parent onboarding</h1><p>Step {{step()+1}} of {{steps.length}}: {{steps[step()]}}</p><progress [value]="step()+1" [max]="steps.length"></progress><div class="mt-4" aria-live="polite" *ngIf="form.invalid">Please complete required fields before continuing.</div><label class="block mt-4">Parent display name<input class="w-full rounded border p-3" formControlName="parentName"></label><label class="block mt-4">Student preferred name<input class="w-full rounded border p-3" formControlName="studentName"></label><label class="block mt-4"><input type="checkbox" formControlName="consent"> I have authority to provide parental consent and accept privacy and terms.</label><div class="mt-6 flex gap-3"><button type="button" (click)="back()">Back</button><button type="button" (click)="next()">Save and continue</button></div></form>`})export class ParentOnboardingComponent{steps=['Profile','Privacy and terms','Parental consent','Student profile','Preferences','Assessment','Campaign','Champion','Review'];step=signal(0);form=new FormBuilder().nonNullable.group({parentName:['',Validators.required],studentName:['',Validators.required],consent:[false,Validators.requiredTrue]});back(){this.step.set(Math.max(0,this.step()-1));}next(){if(this.form.invalid)return;this.step.set(Math.min(this.steps.length-1,this.step()+1));sessionStorage.setItem('mua-parent-onboarding',JSON.stringify(this.form.getRawValue()));}}
