import { Component, inject } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { Router } from '@angular/router';import { AuthService } from '../../../core/auth/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<form class="card mx-auto max-w-md" [formGroup]="form" (ngSubmit)="submit()" novalidate><h1 class="text-3xl font-bold">Sign in</h1><label class="mt-4 block">Email<input class="mt-1 w-full rounded border p-3" type="email" formControlName="email" autocomplete="email"></label><label class="mt-4 block">Password<input class="mt-1 w-full rounded border p-3" type="password" formControlName="password" autocomplete="current-password"></label><button class="mt-6 rounded-full bg-primary-500 px-5 py-3 text-white" [disabled]="form.invalid">Sign in securely</button><p aria-live="polite" class="mt-3 text-red-700">{{error}}</p></form>`})export class LoginPageComponent{private fb=inject(FormBuilder);private auth=inject(AuthService);private router=inject(Router);error='';form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]],password:['',Validators.required]});submit(){if(this.form.invalid)return;this.auth.login(this.form.getRawValue()).subscribe({next:r=>void this.router.navigate([r.user.roles.includes('student')?'/student':'/parent']),error:()=>this.error='Sign in failed. Check your details or contact support.'});}}
