import { describe, expect, it } from 'vitest';
describe('daily learning progression policy',()=>{it('requires activity validation before completion endpoint semantics',()=>{const session={requiredValidationPassed:false};expect(session.requiredValidationPassed).toBe(false);});});
