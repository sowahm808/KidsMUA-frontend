export type LessonStep='opening'|'phonics'|'vocabulary'|'reading'|'comprehension'|'writing'|'scripture'|'closing';export interface DailyLearningSession{id:string;studentId:string;day:number;capsule:number;step:LessonStep;completedSteps:LessonStep[];requiredValidationPassed:boolean;etag?:string;}export interface StudentDashboard{displayName:string;day:number;capsule:number;progressPercent:number;starsToday:number;totalStars:number;badges:string[];weeklyGoal:string;recentEncouragement:string;upcomingMilestone:string;rewardStatus:string;}
