import { CommonModule } from '@angular/common';import { Component, output, signal } from '@angular/core';
@Component({standalone:true,selector:'app-phonics-activity',imports:[CommonModule],template:`<section aria-labelledby="phonics-title"><h2 id="phonics-title" class="text-2xl font-bold">Phonics: letter sound matching</h2><p>Choose the word that begins with the /b/ sound.</p><div class="mt-3 flex flex-wrap gap-3"><button type="button" class="rounded border p-4" *ngFor="let option of options" (click)="answer.set(option)">{{option}}</button></div><button class="mt-4 rounded bg-primary-500 px-4 py-3 text-white" (click)="submit()">Submit answer</button><p aria-live="polite" class="mt-3">{{feedback()}}</p></section>`})export class PhonicsActivityComponent{completed=output<{correct:boolean;activityId:string}>();options=['ball','sun','cat'];answer=signal('');feedback=signal('');submit(){const correct=this.answer()==='ball';this.feedback.set(correct?'Great listening! Ball begins with /b/.':'Try again. Listen for the first sound.');if(correct)this.completed.emit({correct,activityId:'phonics-letter-sound'});}}
