import { CommonModule } from '@angular/common';import { Component, input, signal } from '@angular/core';
@Component({standalone:true,selector:'app-reading-passage',imports:[CommonModule],template:`<article [class.dyslexia-mode]="dyslexia()" [style.font-size.%]="textSize()"><h2 class="text-2xl font-bold">{{title()}}</h2><p class="text-sm">Reading level: {{level()}} · Estimated time: {{minutes()}} minutes</p><button type="button" (click)="textSize.set(textSize()+10)">Increase text</button><button type="button" (click)="dyslexia.set(!dyslexia())">Dyslexia-friendly mode</button><p class="mt-4 leading-loose">{{text()}}</p><p *ngIf="scriptureConnection()" class="mt-4 rounded bg-primary-50 p-3">Scripture connection: {{scriptureConnection()}}</p></article>`})export class ReadingPassageComponent{title=input('A Helpful Friend');level=input('Early reader');text=input('Mia helped Ben read a new word. They prayed, tried again, and celebrated progress together.');scriptureConnection=input('Encourage one another.');textSize=signal(100);dyslexia=signal(false);minutes=signal(2);}
