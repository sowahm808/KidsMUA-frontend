import { Component, inject, OnInit, signal } from '@angular/core';import { PhonicsActivityComponent } from '../components/phonics-activity.component';import { ReadingPassageComponent } from '../components/reading-passage.component';import { DailyLearningStore } from '../state/daily-learning.store';import { LessonStep } from '../models/learning.models';
@Component({standalone:true,imports:[PhonicsActivityComponent,ReadingPassageComponent],template:`<section class="card"><h1 class="text-3xl font-bold">Daily learning player</h1><div role="progressbar" [attr.aria-valuenow]="store.progress()" aria-valuemin="0" aria-valuemax="100" class="my-4 h-3 rounded bg-slate-200"><span class="block h-3 rounded bg-primary-500" [style.width.%]="store.progress()"></span></div><nav class="flex flex-wrap gap-2" aria-label="Lesson steps"><button class="rounded border px-3 py-2" *ngFor="let s of steps" (click)="current.set(s)">{{s}}</button></nav><p aria-live="polite">{{store.error()}} {{store.saving()?'Saving…':''}}</p>@switch(current()){ @case('opening'){<h2>Opening prayer and declaration</h2><p>Prayer content is loaded from the approved curriculum API for each day.</p><button (click)="completeStep('opening')">I participated</button>} @case('phonics'){<app-phonics-activity (completed)="completeStep('phonics')" />} @case('reading'){<app-reading-passage />} @case('closing'){<h2>Closing prayer and blessing</h2><button (click)="finish()">Complete lesson</button>} @default{<h2>{{current()}}</h2><p>Complete the required activity validation before progress is saved.</p><button (click)="completeStep(current())">Submit activity</button>} }</section>`})export class DailyLearningPlayerComponent implements OnInit{readonly store=inject(DailyLearningStore);readonly steps:LessonStep[]=['opening','phonics','vocabulary','reading','comprehension','writing','scripture','closing'];readonly current=signal<LessonStep>('opening');ngOnInit(){this.store.load();}completeStep(step:LessonStep){this.store.save(step);}finish(){this.store.complete();}}
